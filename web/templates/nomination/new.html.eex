<h2>New nomination</h2>

<%= render "form.html", changeset: @changeset,
                        action: nomination_path(@conn, :create),
                        conn: @conn %>

<%= link "Back", to: nomination_path(@conn, :index) %>
